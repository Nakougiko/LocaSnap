<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="@color/background">

    <!-- Logo -->
    <ImageView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@mipmap/ic_launcher"
        android:contentDescription="Logo LocaSnap"
        android:layout_marginBottom="24dp"/>

    <!-- Titre -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bienvenue sur LocaSnap"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/primary"
        android:layout_marginBottom="16dp"/>

    <!-- Bouton d'ouverture de la caméra -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/open_camera_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📷 Prendre une photo"
        android:textSize="18sp"
        app:cornerRadius="16dp"
        android:backgroundTint="@color/primary"
        android:textColor="@color/white"
        android:padding="12dp"
        android:layout_marginBottom="16dp"/>

    <!-- Bouton pour voir les photos -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/view_photos_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📸 Voir mes photos"
        android:textSize="18sp"
        app:cornerRadius="16dp"
        android:backgroundTint="@color/secondary"
        android:textColor="@color/white"
        android:padding="12dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/open_map_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🗺️ Voir la carte"
        android:textSize="18sp"
        app:cornerRadius="16dp"
        android:backgroundTint="@color/secondary"
        android:textColor="@color/white"
        android:padding="12dp"
        android:layout_marginTop="16dp"/>

</LinearLayout>
